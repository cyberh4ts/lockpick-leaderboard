[project]
name = "lockpick-leaderboard"
version = "0.1.0"
description = "A local lockpicking challenge leaderboard"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "jinja2>=3.1.6",
]
